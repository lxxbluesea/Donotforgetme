<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    tools:context="com.example.donotforgetme.Activities.AddItem">

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:layout_gravity="center_vertical"
            android:gravity="center_horizontal">

            <Button
                android:id="@+id/additem_btn_addnotice"
                android:layout_margin="@dimen/normalmargin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textColor="@color/button_selector"
                android:text="@string/additem_btn_addnotice_text"
                android:drawableTop="@drawable/createnotice_selector"
                android:background="@drawable/normalborder_selector"/>

            <Button
                android:id="@+id/additem_btn_addnote"
                android:layout_margin="@dimen/normalmargin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/additem_btn_addnote_text"
                android:drawableTop="@drawable/createnote_selector"
                android:textColor="@color/button_selector"
                android:background="@drawable/normalborder_selector"/>

        </LinearLayout>
    </ScrollView>

</LinearLayout>
